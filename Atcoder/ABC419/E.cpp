#include <iostream>
#include <vector>

using namespace std;

int main(){
    int N, M, L;
    cin >> N >> M >> L;

    vector<int> A(N);
    for (int i = 0; i < N; ++i) {
        cin >> A[i];
    }

    int result = 0;

    

    cout << result << endl;
    return 0;
}
